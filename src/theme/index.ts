
// handled now manually
import './ktoty/style.scss';

import { Theme } from 'consts/GraphicTheme';

export const initTheme = () => {
}

const getTheme = (theme: Theme) => {

}

const theme = getTheme(Theme.Ktoty);

export default getTheme;